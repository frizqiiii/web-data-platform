# API Documentation

FastAPI auto-generates OpenAPI at `/docs` and `/openapi.json` once
the API is running. In Phase 0 this only describes `/health` and
`/ready` — meaningful API documentation grows alongside real domain
routes starting Phase 1.
