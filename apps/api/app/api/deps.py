"""Shared FastAPI dependencies.

Empty in Phase 0 by design — there is no auth, no tenant context,
and no permission checking yet. Those dependencies are added in
Phase 1 (Identity & Multi-tenancy) and Phase 12 (RBAC enforcement),
not stubbed out here ahead of time with fake logic.
"""
